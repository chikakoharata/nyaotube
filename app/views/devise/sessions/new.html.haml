.form
  .header
    = link_to root_path, class: "link-decoration" do
      nyaotube
  %hr
  .form__container
    .form__title
      ログイン

    = form_with model: @user, url: user_session_path, local: true do |f|
    

      =f.email_field :email, class: "text-field", placeholder: 'メールアドレス'
      =f.password_field :password, autocomplete: "off", class: "text-field", placeholder: 'パスワード'

      =f.submit 'ログイン', class: "submit-btn"

    .title
      アカウントをお持ちで無い方はこちら
      =link_to new_user_registration_path, class: 'links-color' do
        新規会員登録
    .title
      パスワードをお忘れの方はこちら
      = link_to '#' do
        パスワードの確認
    = link_to root_path do
      %button.back-btn トップに戻る