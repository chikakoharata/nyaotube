.form
  .header
    = link_to root_path, class: "link-decoration" do
      nyaotube
  %hr
  .form__container
    .form__title
      新規登録
    .container__title
      会員情報入力
    = form_with model: @user, url: user_registration_path, local: true, method: :post do |f|

      .title
        ニックネーム
      =f.text_field :nickname, class: "text-field", placeholder: '  例）猫田ねこ'
      .title
        メールアドレス
      =f.email_field :email, class: "text-field", placeholder: '  PC・携帯どちらでも可'
      .title
        パスワード
      =f.password_field :password, autocomplete: "off", class: "text-field", placeholder: '7文字以上の半角英数字'
      .title
        パスワード確認
      =f.password_field :password_confirmation, autocomplete: "off", class: "text-field", placeholder:"パスワード確認"

      .title
        「新規登録」のボタンを押すことにより、
        = link_to '#', class: 'links-color' do
          利用規約
        に同意したものとみなします
      =f.submit '新規登録', class: "submit-btn"
    = link_to root_path do
      %button.back-btn トップに戻る